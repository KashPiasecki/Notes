name: 'Scheduled Tests Run'

on:
  schedule:
    - cron: '0 15 * * *'


jobs:
  # unitTest:
  #   name: Run Unit Tests
  #   runs-on: ubuntu-latest
  #   steps:
  #   - uses: actions/checkout@v3
  #   - name: Test
  #     run: dotnet test tests/unitTests -c Release

  integrationTest:
    name: Run Integration Tests
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - name: Test
      run: dotnet test tests/Notes.Api.IntegrationTests -c Release